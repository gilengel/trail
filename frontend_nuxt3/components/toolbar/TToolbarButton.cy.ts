/*
import TToolbarButton from './TToolbarButton.vue'

describe('Component', () => {
  describe('TToolbarButton', () => {
    it('should render a toolbar button', () => {
      cy.mount(TToolbarButton as any, { props: { icon: 'star' } })
      cy.get('[data-cy="ttoolbar-btn"]').should('exist')
    })
  })
})
*/
