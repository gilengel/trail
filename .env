DB_URL=trail-db # this must be the docker name to work in a compose file
DB_NAME=trail
DB_PORT=5432
DB_USER=trail
DB_PASSWORD=trail

DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_URL}:${DB_PORT}/${DB_NAME}?schema=public

FRONTEND_PORT=8080


